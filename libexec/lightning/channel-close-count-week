#!/bin/bash
# lightning channel-close-count-week   (FEAT-3090)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'close_count_week': None}))
" "$result"
