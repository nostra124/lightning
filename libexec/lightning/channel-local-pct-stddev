#!/bin/bash
# lightning channel-local-pct-stddev   (FEAT-3164)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'local_pct_stddev_channel': None}))
" "$result"
