#!/bin/bash
# lightning node-listforwards-fee-avg-weekly   (FEAT-3333)
result=$(lightning-cli listforwards 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'fee_avg_weekly': None}))
" "$result"
