#!/bin/bash
# lightning peer-htlc-max-msat-skewness   (FEAT-3337)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'htlc_max_msat_skewness_peer': None}))
" "$result"
