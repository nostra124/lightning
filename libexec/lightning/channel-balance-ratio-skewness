#!/bin/bash
# lightning channel-balance-ratio-skewness   (FEAT-3478)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'balance_ratio_skewness': None}))
" "$result"
