#!/bin/bash
# lightning channel-receivable-stddev   (FEAT-2831)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json, math
data = json.loads(sys.argv[1])
channels = data.get('channels', [])
vals = [ch['receivable_msat'] for ch in channels if 'receivable_msat' in ch]
if not vals:
    print(json.dumps({'receivable_stddev_msat': 0, 'channel_count': 0}))
    sys.exit(0)
mean = sum(vals) / len(vals)
stddev = math.sqrt(sum((x - mean) ** 2 for x in vals) / len(vals)) if len(vals) > 1 else 0
print(json.dumps({'receivable_stddev_msat': round(stddev, 4), 'channel_count': len(vals)}))
" "$result"
