#!/bin/bash
# lightning node-invoice-count-expired   (FEAT-3112)
result=$(lightning-cli listinvoices 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'invoice_count_expired': None}))
" "$result"
