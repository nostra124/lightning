#!/bin/bash
# lightning wallet-address-list   (FEAT-738)
result=$(lightning-cli listaddresses 2>/dev/null) || { echo '[]'; exit 0; }
python3 -c "import sys, json; d=json.loads(sys.argv[1]); print(json.dumps(d.get('addresses', d)))" "$result"
