#!/bin/bash
# lightning node-listpays-preimage <payment_hash>   (FEAT-1841)
if [ $# -lt 1 ]; then echo '{"error":"usage: node-listpays-preimage <payment_hash>"}'; exit 0; fi
phash="$1"
result=$(lightning-cli listpays "$phash" 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
pays = data.get('pays', [])
if not pays: print(json.dumps({'error': 'pay not found'})); sys.exit(0)
p = pays[0]
print(json.dumps({'payment_hash': p.get('payment_hash'), 'payment_preimage': p.get('payment_preimage')}))
" "$result"
