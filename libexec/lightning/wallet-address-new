#!/bin/bash
# lightning wallet-address-new   (FEAT-763)
result=$(lightning-cli newaddr 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "import sys, json; d=json.loads(sys.argv[1]); print(json.dumps({'address': d.get('bech32') or d.get('p2sh-segwit')}))" "$result"
