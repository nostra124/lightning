#!/bin/bash
# lightning peer-local-reserve-max   (FEAT-3106)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'local_reserve_max_msat': None}))
" "$result"
