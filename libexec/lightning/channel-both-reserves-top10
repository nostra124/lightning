#!/bin/bash
# lightning channel-both-reserves-top10   (FEAT-3377)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'top10_both_reserves': None}))
" "$result"
