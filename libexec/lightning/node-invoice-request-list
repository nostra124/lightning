#!/bin/bash
# lightning node-invoice-request-list   (FEAT-804)
result=$(lightning-cli listinvoicerequests 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
requests = data.get('invoicerequests', [])
print(json.dumps({'count': len(requests), 'requests': requests}))
" "$result"
