#!/bin/bash
# lightning peer-channel-count-median   (FEAT-3189)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'channel_count_median_peer': None}))
" "$result"
