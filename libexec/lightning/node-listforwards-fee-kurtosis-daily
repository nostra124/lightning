#!/bin/bash
# lightning node-listforwards-fee-kurtosis-daily   (FEAT-3462)
result=$(lightning-cli listforwards 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'fee_kurtosis_daily': None}))
" "$result"
