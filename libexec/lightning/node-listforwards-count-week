#!/bin/bash
# lightning node-listforwards-count-week   (FEAT-3062)
result=$(lightning-cli listforwards 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'forward_count_week': None}))
" "$result"
