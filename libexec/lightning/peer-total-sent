#!/bin/bash
# lightning peer-total-sent <peer_id>   (FEAT-1257)
if [ $# -lt 1 ]; then echo '{"error":"usage: peer-total-sent <peer_id>"}'; exit 0; fi
peer_id="$1"
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
pid = sys.argv[2]
channels = [c for c in data.get('channels', []) if c.get('peer_id') == pid]
total_sent = sum(c.get('out_fulfilled_msat', 0) for c in channels if isinstance(c.get('out_fulfilled_msat'), int))
print(json.dumps({'peer_id': pid, 'total_sent_msat': total_sent}))
" "$result" "$peer_id"
