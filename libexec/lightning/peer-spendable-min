#!/bin/bash
# lightning peer-spendable-min   (FEAT-2593)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
channels = data.get('channels', [])
peers = {}
for c in channels:
    pid = c.get('peer_id', '')
    val = int(c.get('spendable_msat', 0))
    peers[pid] = peers.get(pid, 0) + val
if not peers:
    print(json.dumps({'peer_id': None, 'spendable_msat': 0}))
else:
    pid = min(peers, key=lambda p: peers[p])
    print(json.dumps({'peer_id': pid, 'spendable_msat': peers[pid]}))
" "$result"
