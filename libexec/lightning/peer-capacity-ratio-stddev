#!/bin/bash
# lightning peer-capacity-ratio-stddev   (FEAT-3026)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'capacity_ratio_stddev_peer': None}))
" "$result"
