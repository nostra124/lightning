#!/bin/bash
# lightning node-invoice-amount-skewness   (FEAT-3421)
result=$(lightning-cli listinvoices 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'invoice_amount_skewness': None}))
" "$result"
