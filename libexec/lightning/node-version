#!/bin/bash
# lightning node-version   (FEAT-651)
result=$(lightning-cli getinfo 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "import sys, json; d=json.loads(sys.argv[1]); print(json.dumps({'version': d.get('version')}))" "$result"
