#!/bin/bash
# lightning peer-htlc-min-msat-kurtosis   (FEAT-3328)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'htlc_min_msat_kurtosis_peer': None}))
" "$result"
