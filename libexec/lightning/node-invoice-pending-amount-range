#!/bin/bash
# lightning node-invoice-pending-amount-range   (FEAT-3246)
result=$(lightning-cli listinvoices 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'pending_amount_range': None}))
" "$result"
