#!/bin/bash
# lightning channel-remote-pct-range   (FEAT-3147)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'remote_pct_range_channel': None}))
" "$result"
