#!/bin/bash
# lightning peer-spendable-balance-kurtosis-yearly   (FEAT-3566)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'spendable_balance_kurtosis_yearly_peer': None}))
" "$result"
