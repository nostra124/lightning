#!/bin/bash
# lightning peer-htlc-count-stddev   (FEAT-3046)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'htlc_count_stddev_peer': None}))
" "$result"
