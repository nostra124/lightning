#!/bin/bash
# lightning peer-remote-balance-range   (FEAT-3247)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'remote_balance_range_peer': None}))
" "$result"
