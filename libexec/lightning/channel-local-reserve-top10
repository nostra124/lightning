#!/bin/bash
# lightning channel-local-reserve-top10   (FEAT-3371)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'top10_local_reserve': None}))
" "$result"
