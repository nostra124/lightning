#!/bin/bash
# lightning node-invoice-unpaid-count-weekly   (FEAT-3562)
result=$(lightning-cli listinvoices 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'unpaid_count_weekly': None}))
" "$result"
