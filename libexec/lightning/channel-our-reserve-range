#!/bin/bash
# lightning channel-our-reserve-range   (FEAT-3262)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'our_reserve_range_channel': None}))
" "$result"
