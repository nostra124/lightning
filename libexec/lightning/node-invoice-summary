#!/bin/bash
# lightning node-invoice-summary   (FEAT-2107)
result=$(lightning-cli listinvoices 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
invoices = data.get('invoices', [])
paid = sum(1 for i in invoices if i.get('status') == 'paid')
unpaid = sum(1 for i in invoices if i.get('status') == 'unpaid')
expired = sum(1 for i in invoices if i.get('status') == 'expired')
total_msat = sum(i.get('amount_received_msat', 0) for i in invoices if i.get('status') == 'paid')
print(json.dumps({'paid': paid, 'unpaid': unpaid, 'expired': expired, 'total_msat': total_msat}))
" "$result"
