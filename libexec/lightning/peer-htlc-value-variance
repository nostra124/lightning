#!/bin/bash
# lightning peer-htlc-value-variance   (FEAT-3363)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'htlc_value_variance_peer': None}))
" "$result"
