#!/bin/bash
# lightning node-listforwards-fee-avg-monthly   (FEAT-3336)
result=$(lightning-cli listforwards 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'fee_avg_monthly': None}))
" "$result"
