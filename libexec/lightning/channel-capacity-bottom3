#!/bin/bash
# lightning channel-capacity-bottom3   (FEAT-2602)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
channels = data.get('channels', [])
sorted_ch = sorted(channels, key=lambda c: c.get('total_msat', 0))
bottom3 = sorted_ch[:3]
out = [{'channel_id': c.get('channel_id'), 'peer_id': c.get('peer_id'), 'total_msat': c.get('total_msat')} for c in bottom3]
print(json.dumps({'bottom3_channels_by_capacity': out}))
" "$result"
