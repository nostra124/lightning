#!/bin/bash
# lightning channel-opener <channel_id>   (FEAT-1948)
if [ $# -lt 1 ]; then echo '{"error":"usage: channel-opener <channel_id>"}'; exit 0; fi
channel_id="$1"
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
cid = sys.argv[2]
for c in data.get('channels', []):
    if c.get('channel_id') == cid or c.get('short_channel_id') == cid:
        print(json.dumps({'channel_id': cid, 'opener': c.get('opener')}))
        sys.exit(0)
print(json.dumps({'error': 'channel not found'}))
" "$result" "$channel_id"
