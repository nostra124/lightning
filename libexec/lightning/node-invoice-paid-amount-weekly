#!/bin/bash
# lightning node-invoice-paid-amount-weekly   (FEAT-3375)
result=$(lightning-cli listinvoices 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'paid_amount_weekly': None}))
" "$result"
