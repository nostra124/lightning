#!/bin/bash
# lightning peer-remote-reserve-max   (FEAT-3116)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'remote_reserve_max_msat': None}))
" "$result"
