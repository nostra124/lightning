#!/bin/bash
# lightning node-pay-amount-range-monthly   (FEAT-3498)
result=$(lightning-cli listpays 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'amount_range_monthly': None}))
" "$result"
