#!/bin/bash
# lightning node-listforwards-local-fail-count   (FEAT-2541)
result=$(lightning-cli listforwards status=local_failed 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
forwards = data.get('forwards', [])
print(json.dumps({'local_failed_forwards_count': len(forwards)}))
" "$result"
