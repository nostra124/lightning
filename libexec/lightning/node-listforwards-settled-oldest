#!/bin/bash
# lightning node-listforwards-settled-oldest   (FEAT-2391)
result=$(lightning-cli listforwards 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
forwards = data.get('forwards', [])
settled = [f for f in forwards if f.get('status') == 'settled']
if not settled:
    print(json.dumps({'oldest_settled_forward': None}))
    sys.exit(0)
oldest = min(settled, key=lambda f: f.get('received_time', 0))
print(json.dumps({'oldest_settled_forward': oldest}))
" "$result"
