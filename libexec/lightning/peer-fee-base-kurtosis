#!/bin/bash
# lightning peer-fee-base-kurtosis   (FEAT-3229)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'fee_base_kurtosis_peer': None}))
" "$result"
