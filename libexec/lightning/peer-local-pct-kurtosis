#!/bin/bash
# lightning peer-local-pct-kurtosis   (FEAT-3270)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'local_pct_kurtosis_peer': None}))
" "$result"
