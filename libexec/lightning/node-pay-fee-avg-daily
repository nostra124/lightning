#!/bin/bash
# lightning node-pay-fee-avg-daily   (FEAT-3339)
result=$(lightning-cli listpays 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'fee_avg_daily_pay': None}))
" "$result"
