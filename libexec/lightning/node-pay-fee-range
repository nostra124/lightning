#!/bin/bash
# lightning node-pay-fee-range   (FEAT-3158)
result=$(lightning-cli listpays 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'pay_fee_range_msat': None}))
" "$result"
