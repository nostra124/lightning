#!/bin/bash
# lightning node-invoice-paid-amount-median   (FEAT-3142)
result=$(lightning-cli listinvoices 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'paid_amount_median_msat': None}))
" "$result"
