#!/bin/bash
# lightning channel-closingd-sigexchange   (FEAT-1662)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
channels = [c for c in data.get('channels', []) if c.get('state') == 'CLOSINGD_SIGEXCHANGE']
print(json.dumps({'count': len(channels), 'channels': [c.get('channel_id') for c in channels]}))
" "$result"
