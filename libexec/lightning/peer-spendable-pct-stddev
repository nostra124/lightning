#!/bin/bash
# lightning peer-spendable-pct-stddev   (FEAT-3499)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'spendable_pct_stddev_peer': None}))
" "$result"
