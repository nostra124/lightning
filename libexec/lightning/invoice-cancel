#!/bin/bash
# lightning invoice-cancel <label>   (FEAT-473)
if [ $# -lt 1 ]; then
    echo '{"error":"usage: invoice-cancel <label>"}'
    exit 0
fi
label="$1"
result=$(lightning-cli delinvoice "$label" unpaid 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
echo "$result"
