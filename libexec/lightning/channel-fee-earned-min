#!/bin/bash
# lightning channel-fee-earned-min   (FEAT-3423)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'fee_earned_min_ch': None}))
" "$result"
