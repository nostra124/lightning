#!/bin/bash
# lightning node-listforwards-amount-total   (FEAT-3306)
result=$(lightning-cli listforwards 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'forwards_amount_total': None}))
" "$result"
