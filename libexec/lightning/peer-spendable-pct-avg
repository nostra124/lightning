#!/bin/bash
# lightning peer-spendable-pct-avg   (FEAT-3486)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'spendable_pct_avg_peer': None}))
" "$result"
