#!/bin/bash
# lightning peer-fee-earned-median   (FEAT-3083)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'fee_earned_median_msat': None}))
" "$result"
