#!/bin/bash
# lightning peer-receivable-skewness   (FEAT-3460)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'receivable_skewness_peer': None}))
" "$result"
