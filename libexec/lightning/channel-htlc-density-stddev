#!/bin/bash
# lightning channel-htlc-density-stddev   (FEAT-3157)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'htlc_density_stddev': None}))
" "$result"
