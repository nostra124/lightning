#!/bin/bash
# lightning peer-balance-skewness   (FEAT-3346)
result=$(lightning-cli listpeers 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'balance_skewness_peer': None}))
" "$result"
