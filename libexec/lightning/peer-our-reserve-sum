#!/bin/bash
# lightning peer-our-reserve-sum   (FEAT-2873)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
total = sum(c.get('our_reserve_msat', 0) for c in data.get('channels', []))
print(json.dumps({'our_reserve_sum_peer_msat': total}))
" "$result"
