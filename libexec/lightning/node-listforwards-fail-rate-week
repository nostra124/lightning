#!/bin/bash
# lightning node-listforwards-fail-rate-week   (FEAT-3035)
result=$(lightning-cli listforwards 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'fail_rate_week': None}))
" "$result"
