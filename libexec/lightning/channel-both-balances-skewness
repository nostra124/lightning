#!/bin/bash
# lightning channel-both-balances-skewness   (FEAT-3533)
result=$(lightning-cli listpeerchannels 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'both_balances_skewness_msat': None}))
" "$result"
