#!/bin/bash
# lightning node-invoice-pending-newest   (FEAT-3182)
result=$(lightning-cli listinvoices 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'newest_pending_invoice': None}))
" "$result"
