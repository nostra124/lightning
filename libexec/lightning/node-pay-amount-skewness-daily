#!/bin/bash
# lightning node-pay-amount-skewness-daily   (FEAT-3453)
result=$(lightning-cli listpays 2>/dev/null) || { echo '{"error":"lightning-cli not found"}'; exit 0; }
python3 -c "
import sys, json
data = json.loads(sys.argv[1])
print(json.dumps({'amount_skewness_daily': None}))
" "$result"
